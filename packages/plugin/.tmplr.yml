steps:
  - read: pluginName
    prompt: What is the name of your plugin?
    default:
      from: filesystem.rootdir

  - copy: package.tmpl.json
    to: package.json

  - update: README.md

  - remove: package.tmpl.json
  - remove: .tmplr.yml
